// Sort Array of Strings:
// Create a function that takes an array of strings and sorts it in alphabetical order.
const sortAlphabetically = () => {};

// Filter Even Numbers:
// Write a function that takes an array of numbers and returns a new array containing only the even numbers.
const filterEvenNumbers = () => {};

// Sort Array of Objects by Property:
// Given an array of objects with a "name" property, write a function that sorts the array alphabetically based on the "name" property.
const sortObjectsByName = () => {};

// Filter by Category:
// Create a function that takes an array of objects with a "category" property and a category name as input. The function should return a new array containing only the objects that belong to the specified category.
const filterByCategory = () => {};

// Reverse Array of Strings:
// Write a function that takes an array of strings and returns a new array with the strings reversed.
const reverseStrings = () => {};

// Sort Array of Numbers in Descending Order:
// Create a function that takes an array of numbers and sorts it in descending order.
const sortDescending = () => {};

// Filter by Property Value:
// Given an array of objects with a specific property, write a function that takes the property name and a value. The function should return a new array containing only the objects that have the specified property value.
const filterByPropertyValue = () => {};

// Sort Array of Objects by Numeric Property:
// Create a function that takes an array of objects with a numeric property and sorts the array in ascending order based on that property.
const sortObjectsByNumericProperty = () => {};

// Filter by Multiple Categories:
// Write a function that takes an array of objects with a "category" property and an array of category names. The function should return a new array containing only the objects that belong to any of the specified categories.
const filterByMultipleCategories = () => {};

module.exports = {
	sortAlphabetically,
	filterEvenNumbers,
	sortObjectsByName,
	filterByCategory,
	reverseStrings,
	sortDescending,
	filterByPropertyValue,
	sortObjectsByNumericProperty,
	filterByMultipleCategories,
};
